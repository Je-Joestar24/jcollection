import { Link } from "react-router-dom"

export default function AuthActions() {
    return (
        <nav>
            <button>Login</button>
            <button>Signup</button>
        </nav>
    )
}